import Presentacion from './Presentacion';//El programa comienza importando la clase "Presentacion" desde un archivo llamado "Presentacion".

// Función principal
function main(): void {
  //Dentro de la función "main", se crea una nueva instancia de la clase "Presentacion" y se asigna a la variable "presentacion".
  const presentacion = new Presentacion();
  presentacion.iniciar();
}

// Ejecutar la función principal
main();
